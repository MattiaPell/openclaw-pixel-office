version: '3.8'

services:
  dashboard:
    build: .
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    volumes:
      - ./public/sprites:/app/public/sprites
    networks:
      - openclaw-net

networks:
  openclaw-net:
    external: true